diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
index e1ebcd8..49722e1 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
@@ -107,6 +107,12 @@ export const stream = (model, context, options) => {
                             }
                         }
                         if (part.functionCall) {
+                            // Inherit thoughtSignature from the preceding thinking/text block if the
+                            // functionCall part itself does not carry one. Gemini sometimes places the
+                            // signature on the last thinking/text part before the functionCall.
+                            const inheritedSignature = currentBlock?.thinkingSignature
+                                || currentBlock?.textSignature
+                                || undefined;
                             if (currentBlock) {
                                 if (currentBlock.type === "text") {
                                     stream.push({
@@ -137,7 +143,7 @@ export const stream = (model, context, options) => {
                                 id: toolCallId,
                                 name: part.functionCall.name || "",
                                 arguments: part.functionCall.args ?? {},
-                                ...(part.thoughtSignature && { thoughtSignature: part.thoughtSignature }),
+                                ...((part.thoughtSignature || inheritedSignature) && { thoughtSignature: part.thoughtSignature || inheritedSignature }),
                             };
                             output.content.push(toolCall);
                             stream.push({ type: "toolcall_start", contentIndex: blockIndex(), partial: output });
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
index 6b2ed8d..adbd3c7 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
@@ -117,6 +117,12 @@ export const stream = (model, context, options) => {
                             }
                         }
                         if (part.functionCall) {
+                            // Inherit thoughtSignature from the preceding thinking/text block if the
+                            // functionCall part itself does not carry one. Gemini sometimes places the
+                            // signature on the last thinking/text part before the functionCall.
+                            const inheritedSignature = currentBlock?.thinkingSignature
+                                || currentBlock?.textSignature
+                                || undefined;
                             if (currentBlock) {
                                 if (currentBlock.type === "text") {
                                     stream.push({
@@ -146,7 +152,7 @@ export const stream = (model, context, options) => {
                                 id: toolCallId,
                                 name: part.functionCall.name || "",
                                 arguments: part.functionCall.args ?? {},
-                                ...(part.thoughtSignature && { thoughtSignature: part.thoughtSignature }),
+                                ...((part.thoughtSignature || inheritedSignature) && { thoughtSignature: part.thoughtSignature || inheritedSignature }),
                             };
                             output.content.push(toolCall);
                             stream.push({ type: "toolcall_start", contentIndex: blockIndex(), partial: output });
